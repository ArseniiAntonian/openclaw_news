# Keep this file empty (or with only comments) to skip heartbeat API calls.
#
# Add narrow checks below only when you want this agent to watch: LLM
# scoring error rate, 429 frequency, объекты без search_description,
# просадку recall на регрессионном тесте между прогонами.